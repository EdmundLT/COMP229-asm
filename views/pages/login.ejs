<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body class="container mx-auto">
    <header><%- include('../partials/header'); %></header>

    <div class="container mx-auto text-center md:p-20 m-20">
      <p class="text-4xl">Login</p>
      <p class="text-red-700 text-lg font-semibold"><%- caution -%> </p>
      <div class="w-1/3 mx-auto pt-4">
        <form action="/auth" method="post">
          <div class="mb-4">
            <label for="username" class="block mb-2 text-sm font-medium"
              >Username</label
            >
            <input
              type="username"
              id="username"
              name="username"
              class="border border-gray-300 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:border-gray-600"
              required
            />
          </div>
          <div class="mb-4">
            <label for="password" class="block mb-2 text-sm font-medium"
              >Password</label
            >
            <input
              type="password"
              id="password"
              name="password"
              class="border border-gray-300 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:border-gray-600"
              required
            />
          </div>
          <div class="pt-4">
            <button
              formaction="/auth"
              type="submit"
              class="text-black hover:bg-green-500 border-solid border-2 border-black py-2 px-4 rounded"
            >
              Login
            </button>
          </div>
        </form>
      </div>
    </div>
    <footer><%- include('../partials/footer'); %></footer>
  </body>
</html>
